---
import MainLayout from '@/layouts/TheLayout.astro'
import TheMain from '@/components/sections/TheMain.astro'
import Bento from '@components/sections/Bento/Bento'
import { sortedPosts, tags } from '@/helpers/getPosts'
const allPosts = sortedPosts.map(post => ({slug: post.slug, data: post.data }) )
---

<MainLayout
  title='Publicaciones sobre programación'
  description='React, javascript, css, html.'
>
  <TheMain>
    <Bento client:idle posts={allPosts} tags={tags} title='Bento' />
  </TheMain>
</MainLayout>